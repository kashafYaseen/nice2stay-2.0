<tr>
  <td class="align-middle">
    <a class="text-medium navi-link" href="#" data-toggle="modal" data-target="#orderDetails">
      <%= reservation.id %>
    </a>
  </td>

  <td class="align-middle"><%= link_to reservation.lodging_name, reservation.lodging %></td>
  <td class="align-middle"><%= render_date reservation.created_at %></td>
  <td class="align-middle">
    <%= render_request_status reservation.request_status %>
  </td>

  <td class="align-middle"><span class="text-medium">$<%= reservation.total_price %></span></td>
</tr>
