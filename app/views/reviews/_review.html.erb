<!-- Review-->
<div class="comment">
  <div class="comment-body">
    <div class="comment-header d-flex flex-wrap justify-content-between">
      <h3 class="comment-title default-font-family"><%= review.title %></h3>
      <div class="mb-2">
        <div class="product-rating text-warning">
          <% review.average_stars.times do |star| %>
            <i class="material-icons star"></i>
          <% end %>
        </div>
      </div>
    </div>

    <p class="comment-text"><%= review.description %></p>
    <div class="comment-footer">
      <span class="comment-meta"><%= review.user_full_name unless review.anonymous? %></span>
      <span class="comment-meta float-right"><%= render_date review.check_in %></span>
    </div>
  </div>
</div>
