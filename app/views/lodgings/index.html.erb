<% content_for :body_class, "pr-3" %>

<div class="row">
  <div id="loader">
    <%= image_tag 'loader.gif', class: 'w-50' %>
  </div>

  <div class="col-md-2 homepage col-sm-8 search-filters pr-0">
    <%= render 'filters', lodgings: @lodgings, amenities: @amenities, experiences: @experiences %>
  </div>

  <div class="col-lg-7 col-md-8">
    <div class="row">
      <div class="seo-data">
        <%= render 'seo_data', custom_text: @custom_text %>
      </div>

      <div class="col-md-12 ">
        <div class="row mb-2 lodgings-list" id="lodgings-list">
          <%= render 'lodgings_list', lodgings: @lodgings %>
        </div>
      </div>

      <div class="col-lg-12 mb-5">
        <div class="pagination-info text-right">
          <%= page_entries_info(@lodgings, entry_name: 'Lodging' ) %>
        </div>

        <div class="pagination">
          <%= paginate @lodgings, remote: true, window: 3 %>
        </div>
      </div>
    </div>
  </div>

  <div class="col-lg-3 col-md-4 fixed-map">
    <div class="sidebar-toggle-map position-right">
      <i class="material-icons place"></i>
    </div>

    <aside class="sidebar sidebar-offcanvas-map position-right">
      <span class="sidebar-close sidebar-close-map"><i class="material-icons icon_close"></i></span>

      <%= tag.div nil, id: 'map' %>
    </aside>
  </div>
</div>

<%= content_for :js_init do %>
  Lodging.init();
  Map.init();
  $('#loader').hide();
<% end %>

<%= content_for :footer_class do 'd-none' end %>
