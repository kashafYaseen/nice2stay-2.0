<%= tag.div class: "lodgings-list-json", data: { lodgings: lodgings.to_json(methods: [:address], only: [:id, :latitude, :longitude, :name, :slug]), bounds: params[:bounds], ids: lodgings.pluck(:id), markers: jeo_json(lodgings) } %>
<%= render lodgings %>
